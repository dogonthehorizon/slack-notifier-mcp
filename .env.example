# Slack Notifier MCP Server Configuration
# Copy this file to .env and fill in your actual values

# Required: Slack Bot Token
# Get this from your Slack app settings:
# 1. Go to https://api.slack.com/apps
# 2. Create a new app or select an existing one
# 3. Go to "OAuth & Permissions" in the sidebar
# 4. Add the following OAuth scopes under "Bot Token Scopes":
#    - chat:write (required for sending messages)
#    - chat:write.public (optional, for posting to channels without joining)
# 5. Install the app to your workspace
# 6. Copy the "Bot User OAuth Token" (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Required: Slack Channel
# The channel where progress notifications will be sent
# Can be a channel name (without #) or channel ID
# Examples:
#   - general (channel name)
#   - alerts (channel name)
#   - C1234567890 (channel ID)
SLACK_CHANNEL=general

# Optional: Python logging level (DEBUG, INFO, WARNING, ERROR)
# Uncomment to override the default INFO level
# PYTHONPATH=src

# Optional: Additional environment variables for development
# UV_INDEX=https://pypi.org/simple/
